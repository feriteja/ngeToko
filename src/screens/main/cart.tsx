import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const cart = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default cart;

const styles = StyleSheet.create({});
